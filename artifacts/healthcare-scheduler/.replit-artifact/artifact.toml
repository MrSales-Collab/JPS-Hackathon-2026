kind = "web"
previewPath = "/"
title = "Healthcare Scheduler"
version = "1.0.0"

[[services]]
localPort = 5000
name = "Healthcare Scheduler"
paths = ["/"]

[services.development]
run = "streamlit run app.py"
